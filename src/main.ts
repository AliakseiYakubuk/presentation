// import { About } from './components/About';
import { Home } from './components/Home';
// import { Skills } from './components/Skills';
import './style.css';

(Home.init().then((view) => view.draw()));
// (Skills.init().then((view) => view.draw()));
// (About.init().then((view) => view.draw()));
